/**
 * Google Places wrapper stub (Person 1).
 * Cache results — do not call twice for the same lat/lng.
 */
export async function reverseGeocodePlaceType(_lat, _lng) {
  return { name: null, place_type: "unknown" };
}
