/**
 * Schema reference for backend.
 * Prefer importing constants from ../../shared/mongodb-schema.js
 * (or copy enums if Node path resolution is awkward in your setup).
 */
export {
  COLLECTIONS,
  NODE_PLACE_TYPES,
  TRAVEL_MODES,
  INDEXES,
  FRIEND_STATUSES,
  GROUP_INVITE_STATUSES,
  VISITED_CELL_RESOLUTION,
} from "../../../shared/mongodb-schema.js";
